Apache is the web server that processes requests and serves web assets and content via HTTP\cf4 \cb5 . MySQL is the database that stores all your information in an easily queried format. PHP is the programming language that works with apache to help create dynamic web content.}
